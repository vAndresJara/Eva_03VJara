<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eva_03vjara">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Eva_03VJara">
        <activity android:name=".Promociones_act"></activity>
        <activity
            android:name=".ui.login.Promociones_act"
            android:label="@string/title_activity_promociones_act" />
        <activity android:name=".listClientes_act" />
        <activity android:name=".Firebase_act" />
        <activity
            android:name=".ui.login.FireBase"
            android:label="@string/title_activity_fire_base" />
        <activity android:name=".Menu_act" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>